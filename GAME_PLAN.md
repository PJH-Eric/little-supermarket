# 小小超市：一頁遊戲企劃

## 一句話賣點

孩子化身小小店員，依照客人的購物清單找商品、放進購物籃，再按下結帳，邊玩邊練習分類、數量、記憶與生活語言。

## 目標與平台

- 目標玩家：3–6 歲幼兒；家長或老師可陪玩。
- 平台：平板優先，其次桌機，再其次手機；支援觸控、滑鼠與鍵盤，直向／橫向皆可操作。
- 一局時間：約 3–8 分鐘，不強迫倒數。
- 模式：單人任務、小幫手挑戰（簡單／普通／困難）、線上 2–4 人合作。

## 核心循環

1. 孩子看到客人的當回合任務卡。
2. 依任務點選貨架上的商品，把正確商品放入購物籃。
3. 商品買對時，數量與小計即時更新；點錯時，清單與狀態不改變，顯示可理解的提示。
4. 任務完成後按「結帳」，得到星星與鼓勵，再進入下一種任務。
5. 完成 3–5 張任務卡後顯示結算，提供再玩一次或返回首頁。

## 任務變化

- 購物清單：依照多個商品與數量買齊客人的清單。
- 分類小達人：找出同一類的所有商品，例如水果或冷藏食品。
- 數數小高手：把指定商品點到正確數量，練習一對一數數。
- 每局固定依序輪替三種任務，第一張先用購物清單教學；內容仍由 seed 決定，方便多人同步與測試。

## 規則與狀態

- 商品種類固定在 8 種；購物清單不重複商品，數量依難度為 1–3 個；分類任務每個商品各 1 個。
- 合法行動：購物清單仍需要該商品時，`collect(productKey)` 增加已購數量與分數。
- 非法行動：商品不在清單或已買足時，狀態保持不變，回傳 `not-needed`。
- 清單完成後狀態為 `ready-checkout`；只有此狀態可執行 `checkout()`。
- 最後一張清單結帳後狀態為 `finished`。
- 線上模式由伺服器驗證所有行動；觀戰者只能讀取狀態與聊天室，不能購物、結帳或準備。

## 難度

- 簡單：3 張清單、每張 2 種商品、數量最多 2；小幫手反應較慢。
- 普通：4 張清單、每張 3 種商品、數量最多 2；小幫手會優先處理缺口最大的商品。
- 困難：5 張清單、每張 4 種商品、數量最多 3；小幫手反應較快並依清單順序處理。

三段難度不改變合法規則，不偷改分數；差異可從清單複雜度與小幫手反應時間觀察。

## MVP 垂直切片

- 首頁、純文字教學、單人完整一局、結算與重新開始。
- 三段小幫手難度、設定 Modal、音效／音樂開關、減少動態與觸控回饋。
- 線上大廳、建立／加入房間、玩家／觀戰角色、邀請連結、準備／開始、合作購物、聊天室與重新連線提示。
- GitHub Pages 靜態部署工作流程；Render Node server、`/health`、CORS 與可設定前端 origin。

## 明確不做

- 不做帳號、永久排行榜、付款、真實商品價格或個人資料收集。
- 房間狀態採記憶體暫存，符合免費 Render 服務可休眠／重啟的限制；不宣稱資料永久保存。

## 資產與外部依賴

- MVP 使用自行繪製的 SVG 商品圖示與 CSS 圖形，不依賴外部圖片授權。
- 音效使用 Web Audio API 產生的短音，沒有外部音訊檔案。
- 線上伺服器依賴 Express 與 `ws`；前端不依賴 CDN。
